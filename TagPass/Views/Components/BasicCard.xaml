<UserControl x:Class="TagPass.Views.Components.BasicCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TagPass.Views.Components"
             mc:Ignorable="d" >
    <Border BorderBrush="Orange" BorderThickness="2" CornerRadius="5" Height="507" Width="733">
        <Grid Background="CornflowerBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 사진 영역 -->
            <Border Grid.Column="0" Background="Black" Margin="5" CornerRadius="5">
                <Image Source="{Binding PersonImage}" Stretch="UniformToFill"/>
            </Border>

            <!-- 텍스트 영역 -->
            <Border Grid.Column="1" Background="Orange" Margin="5" CornerRadius="5">
                <TextBlock Text="{Binding TagInfo}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
