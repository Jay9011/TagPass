<Window x:Class="TagPass.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TagPass"
        xmlns:components="clr-namespace:TagPass.Views.Components"
        xmlns:views="clr-namespace:TagPass.Views"
        mc:Ignorable="d"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Title="TagPass - 출입 모니터링 시스템" Width="1920" Height="1080"
        Background="{StaticResource BackgroundColor}">
    <Grid>
        <!-- 메인 레이아웃 -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 상단 헤더 -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 제목 영역 -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="출입 모니터링 시스템" 
                                   Style="{StaticResource H1}"/>
                        <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='현재 시간: {0:yyyy-MM-dd HH:mm:ss}'}" 
                                   Style="{StaticResource BodyTextStyle}"
                                   xmlns:sys="clr-namespace:System;assembly=mscorlib"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 메인 콘텐츠 영역 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.3*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 카드 영역 -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}"
                        Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 카드 헤더 -->
                        <Border Grid.Row="0" 
                                Background="{StaticResource PrimaryColor}" 
                                CornerRadius="8,8,0,0" 
                                Padding="15,10">
                            <TextBlock Text="사원 정보" 
                                       Foreground="White" 
                                       FontWeight="Bold" 
                                       FontSize="16" 
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                        </Border>

                        <!-- 사원 카드 영역 -->
                        <Canvas Grid.Row="1" ClipToBounds="True">
                            <Canvas.Background>
                                <ImageBrush ImageSource="/Resources/Image/holographic_background.jpg" 
                                            Stretch="UniformToFill" 
                                            Opacity="0.4"/>
                            </Canvas.Background>

                            <components:BasicCard x:Name="BasicCard"
                                                  Width="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualWidth}"
                                                  Height="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualHeight}"
                                                  HorizontalAlignment="Stretch" 
                                                  VerticalAlignment="Stretch"/>
                        </Canvas>

                        <!-- 추가 정보 -->
                        <Border Grid.Row="2" 
                                Background="{StaticResource DarkBackgroundColor}" 
                                CornerRadius="8" 
                                Padding="15" 
                                Margin="0,15,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" 
                                           Text="출입 정보" 
                                           Style="{StaticResource H3}" 
                                           Foreground="{StaticResource TextSecondaryColor}"
                                           Margin="0,0,0,10"/>

                                <Grid Grid.Row="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="위치" Style="{StaticResource BodyTextStyle}"/>
                                        <TextBlock Text="{Binding SelectedEmployee.LocationName}" 
                                                   Style="{StaticResource LabelTextStyle}" 
                                                   FontWeight="Bold"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="출입구" Style="{StaticResource BodyTextStyle}"/>
                                        <TextBlock Text="{Binding SelectedEmployee.EqName}" 
                                                   Style="{StaticResource LabelTextStyle}" 
                                                   FontWeight="Bold"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Grid.Row="2" 
                                           Text="{Binding SelectedEmployee.FormattedDate, StringFormat='출입 시간: {0}'}" 
                                           Style="{StaticResource LabelTextStyle}" 
                                           Margin="0,10,0,0"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!-- 가운데 분할선 -->
                <Rectangle Grid.Column="1" 
                           Fill="{StaticResource BorderColor}" 
                           Width="1" 
                           HorizontalAlignment="Center"/>

                <!-- 출입 로그 목록 -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}"
                        Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- 로그 목록 헤더 -->
                        <Border Grid.Row="0" 
                                Background="{StaticResource PrimaryColor}" 
                                CornerRadius="8,8,0,0" 
                                Padding="15,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                           Text="시간" 
                                           Foreground="White" 
                                           FontWeight="Bold" 
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="1" 
                                           Text="이름" 
                                           Foreground="White" 
                                           FontWeight="Bold" 
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="2" 
                                           Text="소속" 
                                           Foreground="White" 
                                           FontWeight="Bold" 
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="3" 
                                           Text="상태" 
                                           Foreground="White" 
                                           FontWeight="Bold" 
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="4" 
                                           Text="위치" 
                                           Foreground="White" 
                                           FontWeight="Bold" 
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- 로그 목록 -->
                        <ListBox Grid.Row="1" 
                                x:Name="AccessLogListBox"
                                Background="Transparent" 
                                BorderThickness="0"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                ItemsSource="{Binding AccessLogs}"
                                SelectedItem="{Binding SelectedEmployee}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="Transparent" 
                                            Padding="15,10" 
                                            BorderThickness="0,0,0,1" 
                                            BorderBrush="{StaticResource BorderColor}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="200"/>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- 시간 -->
                                            <TextBlock Grid.Column="0" 
                                                       Text="{Binding FormattedDate}" 
                                                       Style="{StaticResource BodyTextStyle}"
                                                       HorizontalAlignment="Center"
                                                       FontFamily="Consolas"/>

                                            <!-- 이름 -->
                                            <StackPanel Grid.Column="1" Margin="10,0">
                                                <TextBlock Text="{Binding Name}" 
                                                           Style="{StaticResource BodyTextStyle}" 
                                                           HorizontalAlignment="Center"
                                                           FontWeight="Bold"/>
                                                <TextBlock Text="{Binding Sabun}" 
                                                           Style="{StaticResource LabelTextStyle}" 
                                                           HorizontalAlignment="Center"
                                                           FontSize="11"/>
                                            </StackPanel>

                                            <!-- 소속 -->
                                            <TextBlock Grid.Column="2" 
                                                       Text="{Binding OrgName}" 
                                                       Style="{StaticResource BodyTextStyle}"
                                                       HorizontalAlignment="Center"
                                                       Margin="10,0"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <!-- 상태 -->
                                            <Border Grid.Column="3" 
                                                    CornerRadius="10" 
                                                    Padding="8,3"
                                                    Margin="10,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding State}" Value="IN">
                                                                <Setter Property="Background" Value="{StaticResource SuccessColor}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding State}" Value="OUT">
                                                                <Setter Property="Background" Value="{StaticResource WarningColor}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding StateName}" 
                                                           Foreground="White" 
                                                           FontWeight="Bold" 
                                                           VerticalAlignment="Center"
                                                           HorizontalAlignment="Center" 
                                                           FontSize="11"/>
                                            </Border>

                                            <!-- 위치 -->
                                            <TextBlock Grid.Column="4" 
                                                       Text="{Binding EqName}" 
                                                       Style="{StaticResource LabelTextStyle}"
                                                       Margin="10,0"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource PrimaryLightColor}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SecondaryLightColor}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Grid>
                </Border>

            </Grid>

            <!-- 하단 상태바 -->
            <Border Grid.Row="2" 
                    Style="{StaticResource CardStyle}" 
                    Margin="0,15,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                               Text="{Binding AccessLogs.Count, StringFormat='총 {0}건의 출입 기록'}" 
                               Style="{StaticResource BodyTextStyle}" 
                               VerticalAlignment="Center"/>

                    <Border Grid.Column="1" 
                            Background="{StaticResource SuccessColor}" 
                            CornerRadius="5" 
                            Padding="8,3" 
                            Margin="10,0">
                        <TextBlock Text="출입: 3" 
                                   Foreground="White" 
                                   FontWeight="Bold"/>
                    </Border>

                    <Border Grid.Column="2" 
                            Background="{StaticResource WarningColor}" 
                            CornerRadius="5" 
                            Padding="8,3" 
                            Margin="10,0">
                        <TextBlock Text="퇴실: 2" 
                                   Foreground="White" 
                                   FontWeight="Bold"/>
                    </Border>

                    <TextBlock Grid.Column="3" 
                               Text="F12: 설정 | F5: 콘솔" 
                               Style="{StaticResource LabelTextStyle}" 
                               VerticalAlignment="Center" 
                               Margin="20,0,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <!-- 설정 뷰 -->
        <views:SettingsView x:Name="SettingsView" 
                            Visibility="Collapsed"
                            SettingsClosed="SettingsView_SettingsClosed"/>

        <!-- 콘솔 뷰 -->
        <views:ConsoleView x:Name="ConsoleView" 
                           Visibility="Collapsed"
                           ConsoleClosed="ConsoleView_ConsoleClosed"/>

    </Grid>
</Window>
